# 차티(Charty) 기획서

> 개인 개발 로드맵용 문서. 코드 기준: `charty-web` v0.1.0 (2026-08 기준).
> 화면별 상세 명세는 [`design-prompts/`](../design-prompts/) 참조.

---

## 1. 제품 개요

**차티는 과거 차트 데이터로 빠르게 모의투자를 반복하는 연습 앱이다.**

### 문제의식
- 실전으로 거래 스타일을 검증하려면 시간이 오래 걸리고 비싸다. 스윙 한 사이클에 몇 주, 장기 포지션은 몇 달이 필요하다.
- 지나간 차트로 복기할 때는 이미 결과를 알고 본다. "저기서 샀어야지"는 연습이 아니다.

### 목표
1. **자신에게 맞는 거래 스타일 찾기** — 단타/스윙/장기를 짧은 시간 안에 여러 번 경험하고, 기록으로 비교한다.
2. **당시 상황을 객관적으로 보는 시각** — 종목과 시점을 숨긴 차트 앞에서, 결과를 모르는 상태로 판단하는 훈련.

### 타깃
개인 트레이딩 연습자(우선은 개발자 본인). 서버 없이 브라우저만으로 동작.

---

## 2. 핵심 컨셉

| 컨셉 | 설명 |
|---|---|
| **시간 압축** | 과거 실데이터의 랜덤 구간을 [다음 캔들] 버튼으로 한 캔들씩 진행. 60일짜리 거래를 몇 분 안에 경험 |
| **블라인드** | 종목명을 `******`로 마스킹. 결과와 맥락을 모르는 상태에서 차트만으로 판단하게 강제 |
| **기록과 회고** | 세션 종료마다 감정(5택)·메모를 남기고, 주문마다 매매 이유를 선택. 쌓인 기록으로 스타일별 승률·수익률 비교 |

---

## 3. 현재 구현 현황 (v0.1.0)

### 기술 스택
| 항목 | 선택 |
|---|---|
| 프레임워크 | React 19 + Vite + TypeScript |
| 차트 | lightweight-charts v5 (캔들·거래량·EMA·볼밴·RSI) |
| 상태/저장 | zustand + persist → localStorage 키 `charty`. **서버 없음** |
| 라우팅 | react-router-dom 7, HashRouter (정적 호스팅용) |
| 데이터 수집 | `charty-web/scripts/fetch-data.py` (yfinance, 수동 실행) |
| 테스트 | vitest (`src/lib/engine.test.ts`) |

### 화면 (8개, 전부 구현)
| 라우트 | 화면 | 요약 | 명세 |
|---|---|---|---|
| `/` | 홈 | 총자산·스트릭, 진행중 세션 이어하기, 성과 요약, 자산 추이 | [01](../design-prompts/01-home.md) |
| `/practice` | 연습 모드 | 시뮬레이션 / 스피드 퀴즈(준비중) 선택 | [02](../design-prompts/02-practice-main.md) |
| `/practice/style` | 스타일 선택 | 기본 3스타일 + 커스텀 중 선택 → 시작 | [03](../design-prompts/03-choose-trading-style.md) |
| `/practice/style/custom` | 커스텀 스타일 | 기간 + 타임프레임 조합 생성·편집 | [04](../design-prompts/04-custom-trading-style.md) |
| `/sim` | 시뮬레이션 | 핵심 화면. 아래 참조 | [05](../design-prompts/05-simulation.md) |
| `/review` | 회고 | 결과 + 감정 5택 + 메모 → 기록 저장 | [06](../design-prompts/06-reflection.md) |
| `/history` | 기록 | 승률·평균 수익률, 스타일별 필터, 세션 카드 | [07](../design-prompts/07-history.md) |
| `/more` | 더보기 | 테마, 데이터 초기화, 앱 정보 | [08](../design-prompts/08-more.md) |

### 거래 스타일 프리셋 (`src/lib/data.ts`)
| 스타일 | 거래 기간 | 차트 간격 | 캔들 수 |
|---|---|---|---|
| 단타 | 1일 | 5분 | 78 |
| 스윙 | 14일 | 1시간 | 98 |
| 장기투자 | 60일 | 4시간 | 120 |
| 커스텀 | 자유 (기간×단위) | 5분/1시간/4시간 중 택 1 | 환산 계산 |

### 시뮬레이션 규칙 (`src/lib/engine.ts`)
- 시작 잔고 100만. 지정가 주문만 지원 (다음 캔들의 고가~저가 범위에 들어오면 체결).
- LONG/SHORT 각 1포지션, 추가 진입 시 가중평균 평단. 숏도 진입금액만큼 현금 잠금.
- 마지막 캔들 도달 또는 [End Now] 시 미체결 취소 + 잔여 포지션 종가 청산.
- 레버리지·수수료·슬리피지·시장가 없음 (의도적 단순화).

### 데이터
- `charty-web/public/data/` — 6종목(QQQ·SPY·AAPL·NVDA·TSLA·MSFT) × 3타임프레임(5m·1h·4h) 정적 JSON.
- 세션 시작 시 종목 랜덤 선택 + 지표 워밍업(EMA 200 + 과거 60캔들)을 확보한 랜덤 시작점.
- 4h는 yfinance에 없어 1h 리샘플로 생성.

### 기록 구조 (`src/types.ts`)
- 회고 저장 시 `SimRecord`(스타일·종목·시작/종료 잔고·수익률·거래 수·감정·메모)가 기록에 추가되고 잔고 이월.
- 승률·평균 수익률·스트릭·자산 추이는 기록에서 즉석 계산.
- ⚠️ 개별 체결 내역(`Trade[]`: 단가·수량·손익·매매 이유)은 세션 종료 시 **폐기됨** → [R0-1](#5-로드맵-선행-과제-r0)

---

## 4. 확장 로드맵

우선순위 순. R = Roadmap.

### R1. 멀티 타임프레임 차트
현재는 세션의 차트 간격(예: 장기투자 = 4시간봉) 하나로만 본다.

- **시간 진행은 그대로**: [다음 캔들]은 세션 간격(4시간) 단위로 유지.
- **멈춘 차트에서 타임프레임 전환**: 현재 시점까지의 데이터를 다른 시간봉(15분/1시간/1일 등)으로 다시 그려서 볼 수 있게.
- 필요 작업:
  - 동일 종목·동일 구간의 타 타임프레임 데이터 슬라이싱 (현재 커서 시각 이후는 절대 노출 금지)
  - `Chart.tsx`에 타임프레임 셀렉터 UI
  - 선행: [R0-3] 타임프레임 데이터 확충

### R2. 시뮬레이션 News 기능
시뮬레이션 상단의 News 탭(현재 "준비중")에 **해당 차트 시간대에 실제로 존재했던 경제지표**를 보여준다.

- 내용: 지표별 이전값 / 발표값 (예: 기준금리, FOMC 점도표, CPI, 고용지표).
- **블라인드 정책이 핵심**: 종목을 숨겼는데 뉴스로 시대를 추정할 수 있으면 무의미.
  - 연도·특정 사건명·인물명 등 시대 추정 힌트 제거.
  - 수치와 방향성(이전값 대비 상승/하락)만 노출.
- 필요 작업:
  - 경제지표 히스토리 데이터셋 수집·정제 (정적 JSON, 서버 없음 유지)
  - 시대 추정 힌트 필터링 기준 정의
  - News 탭 UI (`Simulation.tsx`)

### R3. 종합 리포트
기록이 충분히 쌓이면, **"거시경제 상황이 X일 때 거래 스타일 Y가 너에게 잘 맞는다"**를 데이터로 보여준다.

- 승률 높았던 세션들의 공통점 분석: 당시 거시 지표 상태(금리 방향, 지표 서프라이즈 등) × 거래 스타일 × 매매 이유.
- 현재 경제 시장이 그 조건과 유사한지 표시.
- 선행 조건:
  - [R0-1] 세션별 `Trade[]` 보존 — 매매 이유·체결 단위 분석의 원천 데이터
  - R2의 거시경제 데이터셋 — 세션 구간과 지표 상태를 연결

### R4. 스피드 퀴즈 (후순위)
차트를 보고 롱/숏을 즉답하는 퀴즈. `/practice`에 카드만 존재.

- **Win/Lose 판정 기준이 미정**이라 후순위: 몇 캔들 뒤를 기준으로? 등락폭 임계값은? 횡보면?
- 개념 설계가 끝나면 착수.

---

## 5. 로드맵 선행 과제 (R0)

구현 탐색에서 발견된 갭. 로드맵 항목의 전제 조건이다.

| ID | 과제 | 왜 필요한가 |
|---|---|---|
| R0-1 | `SimRecord`에 `Trade[]` 보존 | R3 분석의 원천 데이터. 기록 화면에서 거래별 복기도 가능해짐 |
| R0-2 | 차트 시간축 날짜 마스킹 | 종목은 가리는데 시간축에 실제 연도·날짜가 노출됨 (`Chart.tsx`). 블라인드 정책 위반. R2와 같은 정책으로 처리 |
| R0-3 | 15m/30m/1d/1w 데이터 추가 | R1의 전제. `CustomStyle.tsx`에 칩은 이미 있고 데이터만 없음. `fetch-data.py` 확장 |

---

## 6. 원칙

- **서버 없음 유지 — 단, 수익화 결정 시점까지**: 모든 데이터는 정적 JSON + localStorage. 배포는 정적 호스팅. 구독 도입(→[8. 수익화 방향](#8-수익화-방향))이 확정되면 BaaS 최소 전환.
- **의존성 최소**: 현재 런타임 의존성 5개. 새 기능도 이 기조 유지.
- **블라인드 정책** (숨기는 것 / 보여주는 것):
  - 숨김: 종목명, 실제 날짜·연도, 시대 추정 가능한 사건·인물
  - 노출: 가격 움직임, 지표(EMA/볼밴/RSI/거래량), 경제지표의 수치와 방향성(R2)
  - 세션 완료 후에는 종목명 공개 (현재 구현됨. 날짜도 완료 후 공개로 통일)

---

## 7. 버전 마일스톤

날짜 약속 없음. 순서만 정한다.

| 버전 | 내용 |
|---|---|
| v0.1 | 현재. 시뮬레이션·회고·기록 코어 루프 완성 |
| v0.2 | R0(선행 과제) + R1 멀티 타임프레임 |
| v0.3 | R2 News 기능 |
| v0.4 | R3 종합 리포트 |
| 이후 | R4 스피드 퀴즈, 사용법 가이드 |
| v1.0 | 구독 수익화 — 계정·결제, BaaS 전환 (→8장) |
| v2.0 | 플랫폼화 — 실계좌 연동·노하우 커뮤니티 (→9장) |

---

## 8. 수익화 방향

> 아직 확정 스펙이 아닌 방향. R3까지 완성해 코어 가치를 증명한 뒤 착수.

**결론: 프리미엄 구독 (트레이딩 저널 SaaS 모델).**
유사 검증 사례: TradeZella — 차트 리플레이 연습 + 매매 저널 + 분석 리포트를 월 $20~30 구독으로 판매.

| 구분 | 내용 |
|---|---|
| 무료 | 코어 루프 — 기본 3스타일, 기본 종목, 시뮬레이션·회고·기록 |
| 유료 | **R3 종합 리포트**(킬러 기능), 데이터 확장(코인·국내주식·종목 추가), 커스텀 스타일 무제한, News(R2) |

배제한 수단:
- **광고**: 니치 도구라 트래픽이 안 나오고, 연습 몰입을 해침.
- **일회성 결제**: 데이터를 지속 공급해야 하는 구조와 불일치.

전제 조건: 결제·계정 = 서버 필요. 그 시점에 Supabase류 BaaS로 최소 전환 (6장 원칙의 단서).

---

## 9. 장기 비전: 모의투자 플랫폼

> 아이디어 단계. 진입 순서: 연습앱 완성(→R3) → 구독 수익화 → 플랫폼. 플랫폼은 유저 없이는 콜드스타트로 실패하고, 연습앱은 혼자 써도 가치가 있어 유저 유입 경로가 된다.

### 컨셉
연습 기록 + **API로 검증된 실계좌 거래 기록**을 기반으로 노하우를 공유하는 커뮤니티.
차별점: "자칭 고수" 리딩방과 달리 수익 기록이 증권사·거래소 API로 검증됨 (해외 유사 사례: Collective2, eToro).

### 기능 스케치
- 실계좌 연동: 한국투자증권 KIS Developers, 업비트/바이낸스 API로 체결내역 조회
- 연습(차티 세션) + 실전 기록 통합 뷰
- 노하우 게시글에 검증된 기록 첨부, 팔로우

### ⚠️ 규제 리스크
- 대가를 받고 투자 노하우·종목 판단을 제공하면 **유사투자자문업 신고 대상**이 될 수 있음.
- "따라하기 매매"(카피 트레이딩)까지 가면 **투자일임업 라이선스** 영역.
- 타인의 증권사 API 키를 다루므로 보안·서버 인프라 필수.
- **플랫폼 수익 모델 확정 전에 법률 검토가 선행되어야 함.**
